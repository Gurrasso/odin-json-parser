/*
	
	TODO: Free memory, more checks for invalid syntax 

*/
package json

import "core:fmt"
import "core:strings"

Error :: enum{
	NO_ERROR,

	FILE_READ_FAILED,
	NOT_VALID_JSON_FILE,
	NO_DATA_TO_TOKENIZE,

	TOKEN_LIMIT_EXCEEDED,

	SUBSTRING_FAILED,
	STRING_TO_NUMBER_CONVERSION_FAILED,
	STRING_TO_BOOL_CONVERSION_FAILED,

	INVALID_VALUE_TYPE,
	
	FAILED_PARSE_INVALID_TOKEN_TYPE,
	FAILED_PARSE_NIL_TOKEN_TYPE,

	ID_NOT_PART_OF_MAP,
	
	ERROR_DELETING_VALUE,
}
